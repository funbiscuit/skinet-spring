# keytool -genkeypair -keyalg RSA -keysize 2048 -storetype PKCS12 -keystore dev-ssl.p12 -validity 3650
ssl.p12

# openssl dhparam -out dhparam.pem 2048
dhparam.pem

# openssl req -x509 -nodes -days 3650 -newkey rsa:2048 -keyout nginx-ssl.key -out nginx-ssl.crt
nginx-ssl.crt
nginx-ssl.key
