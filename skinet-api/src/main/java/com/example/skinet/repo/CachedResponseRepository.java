package com.example.skinet.repo;

import com.example.skinet.core.entity.CachedResponse;
import org.springframework.data.repository.CrudRepository;

public interface CachedResponseRepository extends CrudRepository<CachedResponse, String> {
}
